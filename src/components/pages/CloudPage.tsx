import React from 'react';
import FileManager from '../parts/FileManager/FileManager';

const CloudPage = () => {
    return (
        <div>
            <FileManager />
        </div>
    );
};

export default CloudPage;